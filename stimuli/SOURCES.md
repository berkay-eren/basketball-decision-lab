# Stimulus Sources

External media download was attempted from Wikimedia Commons, but network/DNS access to external sources was unavailable in this environment.

The sample stimuli below were generated locally as reusable placeholder assets:

- `sample_01.svg` — locally created SVG placeholder (scene: ball handler with open teammate)
- `sample_02.svg` — locally created SVG placeholder (scene: player near rim with defender)
- `sample_03.svg` — locally created SVG placeholder (scene: wing attack / drive lane)
